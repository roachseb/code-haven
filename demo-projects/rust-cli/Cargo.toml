[package]
name = "demo-rust-cli"
version = "1.0.0"
edition = "2021"
description = "Code Haven demo — Rust CLI tool"

[dependencies]
clap = { version = "4", features = ["derive"] }

[dev-dependencies]
assert_cmd = "2"
predicates = "3"
