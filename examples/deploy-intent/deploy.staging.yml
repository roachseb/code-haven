# deploy.staging.yml — Staging environment overrides

project_id: myorg-staging-789012
region: northamerica-northeast1

replicas:
  min: 1
  max: 3

resources:
  cpu: "1"
  memory: 512Mi

env:
  LOG_LEVEL: info
