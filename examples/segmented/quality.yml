# ── Quality ────────────────────────────────────────────────────
# Linting, code metrics, link checking, Hadolint, actionlint.
name: "\U0001F4CA Quality"

on:
  push:
    branches: [main, develop, 'feature/**', 'release/**', 'hotfix/**']
  pull_request:
    branches: [main, develop]

permissions:
  contents: read
  checks: write

jobs:
  quality:
    uses: roachseb/code-haven/.github/workflows/_orchestrate-quality.yml@main
    secrets: inherit
